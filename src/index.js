const api = require('./api.js"');
/*
const mdLinks = () => {
    const arrayLinks = readingLinks('./data/data.md');
    //console.log(arrayLinks);
    const promiseStatusLinks = makeHttpRequest(arrayLinks);
  
    return promiseStatusLinks.then( statusHttpArray => {
      return arrayLinks.map( (element, index) => {
        return {
          ...element,
          status: statusHttpArray[index]
        }
      })
    });
  };
  
  mdLinks().then(finalArray => console.log(finalArray));*/
  //console.log("Promesa resuelta");